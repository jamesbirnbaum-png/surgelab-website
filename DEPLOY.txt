SURGELAB . NETLIFY DEPLOY PACKAGE
Built 29 July 2026

FILES
  index.html                  Home (self-contained)
  agencies.html               Media agency partner programme
  privacy-policy.html         Privacy policy
  terms-and-conditions.html   Terms & conditions
  _redirects                  Clean URLs: /agencies, /privacy-policy, /terms-and-conditions, /login
  _headers                    Security + cache headers
  robots.txt                  Allows AI answer engines
  sitemap.xml                 4 URLs
  llms.txt                    Plain-language summary for AI crawlers
  favicon.svg                 Static fallback (the live favicon animates in JS)

HOW TO DEPLOY
  Netlify > your site > Deploys > drag this whole folder onto the drop zone.
  Every page is one file with all CSS, fonts, scripts and images inlined, so
  nothing else needs uploading.

STILL TO DO
  1. Create og-image.png at 1200x630 and drop it in this folder
     (all pages already reference https://surgelab.co/og-image.png).
  2. Submit sitemap.xml in Google Search Console and Bing Webmaster Tools.
  3. Have the privacy policy and terms reviewed by a solicitor before relying on them.
  4. Confirm the company registration number and add it to the footer if required.
